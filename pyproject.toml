[project]
name = "opencode-profiles"
version = "0.1.0"
description = "Manage multiple opencode configuration profiles"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["click>=8.0"]

[project.scripts]
opencode-profiles = "opencode_profiles.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
